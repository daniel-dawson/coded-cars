<%= form_with(model: ownership_history, local: true) do |form| %>
  <% if ownership_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ownership_history.errors.count, "error") %> prohibited this ownership_history from being saved:</h2>

      <ul>
        <% ownership_history.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :owner_id %>
    <%= form.text_field :owner_id %>
  </div>

  <div class="field">
    <%= form.label :car_id %>
    <%= form.text_field :car_id %>
  </div>

  <div class="field">
    <%= form.label :admin_id %>
    <%= form.text_field :admin_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
